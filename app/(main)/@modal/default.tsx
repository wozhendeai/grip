/**
 * Default export for modal parallel route
 *
 * Returns null when no intercepted route is active.
 * Required by Next.js for parallel routes to work correctly.
 */
export default function Default() {
  return null;
}
