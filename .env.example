# Database
DATABASE_URL="postgresql://user:password@host:5432/database?sslmode=require"

# better-auth
BETTER_AUTH_SECRET="generate-a-random-secret-at-least-32-chars"
BETTER_AUTH_URL="http://localhost:3000"

# GitHub OAuth
# Create at: https://github.com/settings/developers
GITHUB_CLIENT_ID=""
GITHUB_CLIENT_SECRET=""

# GitHub API Token (for fetching public repo/user data)
# Create at: https://github.com/settings/tokens (no scopes needed for public data)
GITHUB_TOKEN=""

# Turnkey (HSM-backed backend key management for Access Keys)
# 1. Run: pnpm tsx scripts/generate-turnkey-api-key.ts
# 2. Register public key at: https://app.turnkey.com
# 3. Add keys below, then run: pnpm tsx scripts/init-turnkey-wallets.ts
TURNKEY_ORGANIZATION_ID="280d7088-13a7-4605-9767-dba8fbda48d0"
TURNKEY_API_PUBLIC_KEY=""
TURNKEY_API_PRIVATE_KEY=""

# WebAuthn / Passkey
NEXT_PUBLIC_RP_ID="localhost"
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Tempo Blockchain
# CRITICAL: Set NEXT_PUBLIC_TEMPO_NETWORK correctly for each deployment
# testnet.tpay.xyz → testnet
# app.tpay.xyz → mainnet
NEXT_PUBLIC_TEMPO_NETWORK="testnet"

# Network-specific RPC endpoint
TEMPO_RPC_URL="https://rpc.testnet.tempo.xyz"

# Network-specific chain ID
TEMPO_CHAIN_ID="42429"

# Network-specific explorer URL
TEMPO_EXPLORER_URL="https://explore.tempo.xyz"

# Network-specific token addresses
# Testnet: AlphaUSD, BetaUSD, ThetaUSD (fake tokens for testing)
# Mainnet: Real USDC (when available)
TEMPO_USDC_ADDRESS="0x20c0000000000000000000000000000000000001"
TEMPO_PATH_USD_ADDRESS="0x..."  # PathUSD fee token address
